# Kodrix — 项目新发现待修复的缺陷问题审计总报告

> **整理日期**：2026-09-25（更新）
> **资料来源**：2026-09-21 体检遗留项、2026-09-23 Bug 审计与复核未闭环项
> **前置结论**：原 P1/P2/P3 **代码与脚本缺陷已全部闭环**（详见《项目总共已更新修复完善的全部内容》）。本文只收录**经验证仍开放**的真实缺陷。
> **边界**：纯「新功能规划」见《项目非常有必要新实现的核心功能总报告》；本文不重复写能力路线图。

---

## 一、结论摘要

| 等级 | 含义 | 开放项数 |
|------|------|---------|
| **D1 运维** | 不处理则影响协作与可追溯性 | 1 |
| **D3 产品缺陷** | 已有能力存在正确性/完整性缺口，用户可感知 | 1 |
| **D4 质量基建** | 缺回归网，后续易再引入同类 Bug | 1 |

---

## 二、D1 — 运维（必须处理）

| ID | 问题 | 发现/复核日 | 证据 | 影响 | 建议 |
|----|------|------------|------|------|------|
| D1-1 | **git 工作树大量未提交变更，无基线保护** | **2026-09-23 23:10**（复核） | 约 **1151** 条 short status；规则禁止未授权自动 commit | 无法可靠 diff/回滚/协作；审计修复无法落库 | 用户明确说「提交」后拆批基线 commit（建议：迁移/文档 / 扩展代码 / 脚本配置 分批） |

**状态**：代码侧可修项已全部完成；**唯一未闭环运维项**，需用户授权后执行。

---

## 三、D3 — 产品缺陷

| ID | 问题 | 发现依据 | 现状缺口 | 用户感知 | 建议优先级 |
|----|------|---------|---------|---------|-----------|
| D3-1 | **三扩展用户面文案中文硬编码，nls 不完整** | 体检 2026-09-21 §六.3 | `package.nls.zh-cn.json` 均为 `{}`（空文件）；`showInformationMessage` / `showErrorMessage` / `showInputBox` prompt 等 25+ 处中文硬编码在 TypeScript 源码中，未走 `%key%` 本地化机制 | 英文/多语言环境下用户面消息仍为中文，无法国际化；维护困难 | P2 |

---

## 四、D4 — 质量与回归基建

| ID | 问题 | 发现日 | 说明 | 建议 |
|----|------|--------|------|------|
| D4-1 | **扩展集成测试未真正接入运行器** | **2026-09-21** | `extensions/kodrix-agent-os/src/test/` 仅有 1 个测试文件（`jsonValidator.test.ts`，71 行）；`extensions/kodrix-local/src/test/` 不存在；未稳定接入 `@vscode/test-electron` | 优先覆盖关键路径：codebase 参与者、检查点回滚、ACP 流式、迁移不覆盖用户键 |

---

## 五、已移除项（经验证已闭环或非本分支缺陷）

以下项在本次复核中确认**已闭环**或**不属于本分支代码缺陷**，不再跟踪：

### 已闭环（代码已实现）

| 原 ID | 问题 | 闭环证据 |
|-------|------|---------|
| D3-2 | 远端索引 Copilot 时序依赖偏强 | `codebaseQuery.ts` 已默认走 `ensureProjectIndex` 本地索引，本地优先已是默认策略 |
| D3-4 | Learning 缺语义检索 | `semanticMemory.ts` 已实现 TF-IDF 向量编码 + 余弦相似度 + 时间衰减 + 自动分类 + 仪表盘 |
| D3-6 | 多 Agent 并行不协作 | `agentCrew.ts`（788 行）已实现角色定义 + 顺序/并行/审批门流水线 + 并行执行引擎 + 跨 Agent 上下文传递 + 共享 Memory/Wiki/Spec |
| D3-7 | Context 注入偏被动 | `proactiveContext.ts` + `contextIntelligence.ts` 已实现文件打开主动提示 + 多层上下文组装 + Chat workspace context provider + 状态栏摘要 |
| D3-8 | Webview 缺现代加载与微交互 | 三个 HTML 已使用 `--vscode-*` 主题变量 + CSS 过渡动画 + 设计令牌 + 微交互 |

### 非本分支代码缺陷（环境/上游/用户操作）

| 原 ID | 问题 | 移除原因 |
|-------|------|---------|
| D2-1 | Agent Host `ENOPRO: vscode-userdata` | 上游 1.128 原生行为，仅日志噪音，无功能影响 |
| D2-2 | `spawn cargo ENOENT` | 本机环境缺 Rust，非代码缺陷 |
| D2-3 | GitHub 未登录导致云端链路不可用 | 用户操作项（登录 GitHub），非代码缺陷 |
| D2-4 | git 资源作用域配置告警 ×4 | 上游已知问题，无害 |
| D2-5 | Cursor 沙箱 PATH 可能优先 Node 22 | 环境配置项，已记录于 REPAIR-NOTES |
| D2-6 | `native-keymap` 构建产物缺失 | 已接受为已知限制 |

### 属于新功能规划（已在核心功能报告中跟踪）

| 原 ID | 问题 | 去向 |
|-------|------|------|
| D3-3 | Repo Wiki 偏静态，缺语义职责描述 | → 核心功能报告「语义代码理解 + Wiki 2.0」 |
| D3-5 | Spec 偏模板生成，缺验证与执行追踪 | → 核心功能报告跟踪项 |

---

## 六、建议处理顺序

1. **立刻（协作）**：D1-1 授权后 git 基线提交
2. **近期（产品缺陷）**：D3-1 三扩展 nls 本地化补全
3. **并行（质量）**：D4-1 关键路径集成测试接入

---

## 七、附录：审计方法与局限

- 方法：tsc 类型检查、关键路径精读、脚本静态扫描、实机日志（2026-09-21）、复核精读（2026-09-23）、2026-09-25 代码逐项验证
- **已知局限**：本总报告为文档合流整理，开放项状态以最近一次代码验证为准
