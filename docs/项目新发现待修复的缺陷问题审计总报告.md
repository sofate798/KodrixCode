# Kodrix — 项目新发现待修复的缺陷问题审计总报告

> **整理日期**：2026-09-24  
> **资料来源**：2026-09-21 体检遗留项、2026-09-23 Bug 审计与复核未闭环项、体验提升方案中「现状问题」中的缺陷/债务面  
> **前置结论**：原 P1/P2/P3 **代码与脚本缺陷已全部闭环**（详见《项目总共已更新修复完善的全部内容》）。本文只收录**仍开放**的缺陷、环境债、上游噪音与可复现的产品缺口。  
> **边界**：纯「新功能规划」见《项目非常有必要新实现的核心功能总报告》；本文不重复写能力路线图。

---

## 一、结论摘要

| 等级 | 含义 | 开放项数 |
|------|------|---------|
| **D1 运维/资产** | 不修则影响协作与可追溯性 | 1 |
| **D2 环境/上游** | 非本分支逻辑错误，但有日志噪音或 dev 限制 | 4 |
| **D3 产品债务（缺陷面）** | 已有能力存在正确性/完整性缺口，易被用户感知为「坏了」或「半成品」 | 6 |
| **D4 质量基建** | 缺回归网，后续易再引入同类 Bug | 2 |

**不记为缺陷**：按设计未迁能力（CodeGeeX / Keep·Undo / pywebview 等）、需用户登录 GitHub 才能解锁的云端能力本身。

---

## 二、D1 — 运维与资产（必须处理）

| ID | 问题 | 发现/复核日 | 证据 | 影响 | 建议 |
|----|------|------------|------|------|------|
| D1-1 | **git 工作树大量未提交变更，无基线保护** | **2026-09-23 23:10**（复核） | 约 **1151** 条 short status；规则禁止未授权自动 commit | 无法可靠 diff/回滚/协作；审计修复无法落库 | 用户明确说「提交」后拆批基线 commit（建议：迁移/文档 / 扩展代码 / 脚本配置 分批） |

**状态**：代码侧可修项已完；**唯一复核级「未闭环」运维项**。

---

## 三、D2 — 环境与上游行为（非本分支逻辑 Bug，仍建议跟踪）

| ID | 问题 | 发现日 | 性质 | 说明 | 建议 |
|----|------|--------|------|------|------|
| D2-1 | Agent Host `ENOPRO: vscode-userdata` | **2026-09-21** | 上游 1.128 原生 | Agent Host 未注册 userdata FS；读 settings 走容错，**无功能影响**，仅日志噪音 | 优先跟随上游；若必须消噪，可仿 `cliProcessMain` 注册 `FileUserDataProvider`（慎防长期分叉） |
| D2-2 | `spawn cargo ENOENT`（dev 模式） | **2026-09-21** | 环境 | 上游 dev 用 `cargo run` 建 tunnel CLI；本机无 Rust | `winget install Rustlang.Rustup`，或直接用 `build.bat` 打包版（预编译二进制） |
| D2-3 | GitHub 未登录导致云端链路不可用 | **2026-09-21** | 环境 | Copilot 云能力 / 远端索引 / 部分 Tunnels 需登录；**本地模型链路不受影响** | 账号 → 登录 GitHub |
| D2-4 | git 资源作用域配置告警 ×4 | **2026-09-21** | 上游已知 | vscode.git / search 配置读取提示，无害 | 跟踪上游，不必本分支强修 |
| D2-5 | Cursor 沙箱 PATH 可能优先 Node 22 | **2026-09-23** | 环境 | `preinstall` 要求 major=24 且 ≥24.17.0；沙箱装依赖易失败 | 装依赖显式用系统合规 Node（如 `D:\Program\nodejs\node.exe`） |
| D2-6 | `native-keymap` 构建产物缺失 | **2026-09-23** | 环境/依赖 | 目录空；键盘布局降级 | 维持降级或按 REPAIR-NOTES 补编译；**已接受为已知限制** |

---

## 四、D3 — 产品债务（已有能力的缺陷/半成品面）

> 来源：体检后续建议 + 体验方案「现状问题」。这些不是「从零做功能」，而是**现有实现不完整或体验断裂**，用户会感知为缺陷。

| ID | 问题 | 发现依据 | 现状缺口 | 用户感知 | 建议优先级 |
|----|------|---------|---------|---------|-----------|
| D3-1 | **三扩展文案中文硬编码，nls 不完整** | 体检 2026-09-21 §六.3 | 仅补了 localization 贡献消除噪音；字符串未全面 `%key%` 化 | 英/多语言场景体验差、维护困难 | P2 |
| D3-2 | **远端索引跳过时对 Copilot 时序依赖偏强** | 体检 2026-09-21 §六.5 | 本地索引已能兜底，但「本地优先」未成默认策略 | 偶发 codebase 问答空窗/时序困惑 | P1 |
| D3-3 | **Repo Wiki 偏静态扫描，缺语义职责描述** | 体验方案 / Qoder 差距 | Wiki 目录结构有，模块「做什么」弱 | 问答/导航不准，像半成品智慧 | P1 |
| D3-4 | **Learning 仍偏关键词 / JSONL，缺语义检索** | 体验方案 · 语义记忆 | 跨会话沉淀有，相关召回弱 | 「学了但用不上」 | P1 |
| D3-5 | **Spec 偏模板生成，缺验证与执行追踪** | 体验方案 / Kiro 差距 | 三栏工作台有，闭环弱 | Spec「写了不落地」 | P1 |
| D3-6 | **多 Agent 并行但不协作（无编排/通信）** | 体验方案 · Agent Crew | Agents Window 并行会话有，角色流水线无 | 大任务易打架、难汇总 | P1 |
| D3-7 | **Context 注入偏被动，缺打开文件/意图时的主动建议** | 体验方案 · 主动上下文 | Wiki/Memory/Learning 需手动 | 上下文常漏、Token 浪费 | P1 |
| D3-8 | **Webview（Spec / Hub）基础 HTML，缺现代加载与微交互** | 体验方案 · Webview | 功能可用，体感落后竞品 | 「能用但粗糙」 | P2（体验缺陷） |

---

## 五、D4 — 质量与回归基建

| ID | 问题 | 发现日 | 说明 | 建议 |
|----|------|--------|------|------|
| D4-1 | 扩展 e2e / 集成测试未真正接入 | **2026-09-21** | `extensions/*/test` 有骨架，未稳定跑 `@vscode/test-electron` | 优先覆盖：codebase 参与者、检查点回滚、ACP 流式、迁移不覆盖用户键 |
| D4-2 | 复核局限：未实机再跑 Electron UI 全路径 | **2026-09-23 23:10** | 类型检查 + 静态精读为主 | 重大合并前补一轮 `debug.bat` 冒烟清单 |

---

## 六、已关闭（本文不再跟踪，仅索引）

以下在 **2026-09-21 / 2026-09-23 / 2026-09-23 23:10** 已修复，细节见《项目总共已更新修复完善的全部内容》：

- 体检 6 项（chatParticipants、产物新鲜度、tunnel 配置、nls 噪音、git 初始化、gitignore）  
- 审计 P1×5、P2×7、P3 可修×6 + 接受项×2  
- 复核：agentLoop 注释、脚本路径、stderr 上限、客户端新鲜度、product proposed API、eslint/头注释、shim 冲突等  

---

## 七、建议处理顺序

1. **立刻（协作）**：D1-1 授权后 git 基线提交  
2. **本周（体验断裂）**：D3-2 本地索引优先默认策略；D3-5 Spec 执行追踪最小闭环  
3. **两周内（智能债务）**：D3-4 Learning 语义检索；D3-3 Wiki 语义描述；D3-7 主动上下文  
4. **并行（质量）**：D4-1 关键路径集成测试  
5. **按需（环境）**：D2-2 / D2-3；D2-1 跟随上游  

---

## 八、附录：审计方法与局限

- 方法沿用：tsc、关键路径精读、脚本静态扫描、实机日志（2026-09-21）、复核精读（2026-09-23）  
- **已知局限**：本总报告为文档合流整理（2026-09-24），未在整理当日重新跑全量 Electron UI；开放项状态以该日前最近一次报告为准  
