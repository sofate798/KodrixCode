# Kodrix 项目缺陷与 Bug 深度审计报告（复核版）

- **审计日期**：2026-09-23（深夜，复核基准：`docs/项目Bug与缺陷审计报告-2026-09-23.md`）
- **闭环复核**：2026-09-23 23:10（逐项代码核对 + 未修项落地 + tsc 复验）
- **审计范围**：Kodrix 自定义代码（kodrix-local / kodrix-agent-os / kodrix-skills 三扩展源码）、构建与修复脚本、内核自定义文件、git/环境状态
- **审计方法**：三个扩展 TypeScript 类型检查（tsc --noEmit，全量）+ 修复点逐段精读 + 最小复现验证 + 构建脚本静态扫描 + ESLint 全库扫描 + git 工作树核对
- **核心结论**：原复核发现的 P1/P2/P3 **代码与脚本项已全部闭环**；三扩展 `tsc --noEmit` 均 exit 0。系统 Node 已为 v24.21.0（`.nvmrc` 24.17.0 同 major 且更新）。**git 基线提交未自动执行**（需明确授权，见 P2-6）。

---

## 一、P1 高危（阻断性，必须立即处理）

| # | 缺陷 | 位置 | 证据 | 影响 | 修复建议 | 闭环 |
|---|------|------|------|------|---------|------|
| 1 | **kodrix-agent-os 扩展编译失败（注释提前终止）** | `extensions/kodrix-agent-os/src/agent/agentLoop.ts:292` | 原：`tsc` TS1109×4 + TS1005 + TS1161；块注释含 `**/` | 注释提前结束 → 编译失败 → debug 启动中断 | 改为行注释 | ✔ 已改为 `//`；`tsc -p extensions/kodrix-agent-os --noEmit` exit 0 |
| 2 | **Node 版本漂移，依赖安装被锁死** | 环境 / `.nvmrc` / `build/npm/preinstall.ts` | 原实测 v22；现 `D:\Program\nodejs\node.exe`=**v24.21.0**（npm 11.19.0）；`.nvmrc`=24.17.0 | preinstall 要求 major=24 且 ≥24.17.0 | 使用合规 Node 24 | ✔ 本机已合规；注意 Cursor 沙箱 PATH 可能优先其自带 Node 22，安装依赖请显式用系统 Node |

## 二、P2 中危

| # | 缺陷 | 位置 | 证据 | 影响 | 修复建议 | 闭环 |
|---|------|------|------|------|---------|------|
| 3 | **修复脚本硬编码 `D:/minicode` 路径，4 个脚本全部失效** | `scripts/restore-pw-shim.mjs`、`fix-product-json.mjs`、`copy-native-modules.mjs`、`fix-wms.mjs` | 原写死 `D:/minicode` | 迁入 KodrixCode 后脚本指向错误目录 | `import.meta.url` 解析工程根 | ✔ 四脚本均基于 `scripts/` 上一级为 root；copy 的 SRC 可用 `VSCODE_UNPACKED_NODE_MODULES` / argv |
| 4 | **ACP stderr 输出无上限（旧报告修复遗漏）** | `extensions/kodrix-agent-os/src/acp/acpRegistry.ts` | 原仅 stdout 2MB | stderr 无限拼接可撑爆内存 | stderr 上限 + 截断标记 | ✔ `ERROR_MAX_CHARS=1MB` + `appendError` |
| 5 | **客户端产物过期，dev-fast 无客户端新鲜度检查** | `scripts/dev-fast.ps1` | 原仅扩展有 stale 判定 | 内核改动未进运行产物且无提示 | out/main.js ≥ src 最新 | ✔ 新增 `Test-ClientOutFresh`，过期走 `build-fast` |
| 6 | **git 工作树大量未提交变更** | 仓库根 | 复测约 **1151** 条 short status | 无版本保护 | 授权后基线 commit | ⚠ **未自动 commit**（规则：需用户明确说「提交」）；说一声即拆批提交 |

## 三、P3 低危 / 卫生

| # | 缺陷 | 位置 | 说明 | 建议 | 闭环 |
|---|------|------|------|------|------|
| 7 | git 仓库所有权异常（dubious ownership） | 仓库元数据 | 跨用户拷贝导致；已 `safe.directory` 绕过 | 换机需重加例外 | ✔ 接受：环境项，REPAIR/运维侧保持 safe.directory |
| 8 | proposed API 授权不一致 | `package.json` vs `product.json` | agent-os 声明 `settingsEditorRenderer` 但 product 未授 | 补入 product.json | ✔ `kodrix.kodrix-agent-os` 已含 `chatContextProvider` + `settingsEditorRenderer` |
| 9 | 自定义 JS/修复脚本违反仓库规范 | `fix-*.mjs` 等 | `header/header`、`code-no-new-javascript-files` | 允许列表 + 头注释 | ✔ 已入 `.eslint-allowed-javascript-files`；脚本补 MS 头；`test-electron.*` 已不存在 |
| 10 | 内核自定义文件头注释违规 | `kodrixCursorChatHeader.contribution.ts` | ESLint `header/header` | 标准 Copyright 头 | ✔ 已改为标准 `/*!--- Copyright (c) Microsoft... ---*/` |
| 11 | 两个 policy-watcher 脚本逻辑互相冲突 | copy-native vs restore-pw-shim | copy 曾覆盖 shim 为 native loader | 删 copy 中恢复 index.js 段 | ✔ copy 仅复制 `.node`，明确不改 index.js；shim 决策保留 |
| 12 | native-keymap 模块缺失 | `node_modules/native-keymap/build/Release/` | 空目录；键盘布局降级 | 维持降级 | ✔ 接受（与 REPAIR-NOTES 一致） |

## 四、旧报告「已修复」闭环复核

| 编号 | 缺陷 | 复核结果 | 证据 |
|------|------|---------|------|
| P1-1 | 检查点回滚路径穿越 | ✔ 已修复 | `checkpointManager.ts` `resolveSafeRestorePath` |
| P1-2 | ACP JSONL 跨 chunk 截断 | ✔ 已修复 | `acpRegistry.ts` stdoutBuf + close flush |
| P1-3 | CTS 泄漏 | ✔ 已修复 | `agentLoop.ts` try/finally dispose |
| P1-4 | LLM 请求无超时 | ✔ 已修复 | 请求级剩余预算超时 |
| P1-5 | 终端命令误报失败 | ✔ 已修复 | `incomplete` + agentLoop 区分 |
| P2-6 | 快照 dirty 内容 | ✔ 已修复 | 读磁盘已保存内容 |
| P2-7 | diff hunk 头非标准 | ✔ 已修复 | `@@ -1,N +1,M @@` |
| P2-8 | LCS O(n×m) 内存爆炸 | ✔ 已修复 | `DIFF_DP_CELL_LIMIT=2_000_000` |
| P2-9 | 索引写入无原子性 | ✔ 已修复 | `atomicWriteFileSync` |
| P2-10 | 语义搜索全量读索引 | ✔ 已修复 | getProjectIndex/ensureProjectIndex |
| P2-11 | 迁移覆盖用户配置 | ✔ 已修复 | inspect 后仅写未设置键 |
| P2-12 | ACP 输出无上限 | ✔ **已完整修复** | stdout 2MB + **stderr 1MB**（本轮补齐） |
| P3-1~P3-4 | staged 残留/operations 滚动/快照截断/幂等下限 | ✔ 已修复 | 同前 |
| P3-5 | 搜索 glob 支持 | ✔ **回归已消除** | 行注释；tsc 通过；glob 逻辑保留 |
| P3-6 | package.json 缩进/嵌套 | ✔ 已修复 | checkpoint 配置位置正确 |
| P3-7/8 | envDetect / API Key 双通道 | ✔ 复核接受 | 白名单 + SecretStorage |

## 五、环境与资产状态（闭环后）

| 项 | 状态 |
|----|------|
| Electron 运行时 | ✔ `.build/electron/Kodrix.exe` + locales |
| policy-watcher shim | ✔ 立即 callback shim（正确）；copy 不再覆盖 |
| copilot 扩展 | ✔ `extensions/copilot/dist/` 存在 |
| 类型定义 | ✔ `out/vscode-dts/vscode.d.ts` |
| kodrix-local / kodrix-skills 类型检查 | ✔ exit 0 |
| kodrix-agent-os 类型检查 | ✔ exit 0（P1-1 已消） |
| 客户端 out/ 新鲜度 | ✔ dev-fast 已检查；过期自动 transpile |
| 三扩展 package.json / 命令声明 | ✔ |
| Node | ✔ 系统 v24.21.0（满足 ≥24.17.0 major=24） |
| git 工作树 | ⚠ ~1151 条未提交 — 待授权基线 commit |

## 六、建议修复优先级（闭环后）

1. ~~立即：修 agentLoop 注释~~ ✔
2. **待你一句「提交」**：git 基线（可拆迁移/文档两批）
3. ~~本周：脚本路径 / stderr / dev-fast / product API~~ ✔
4. ~~卫生：eslint 允许列表、头注释、shim 冲突~~ ✔

## 附录：本次验证执行方式

- 类型检查：`D:\Program\nodejs\node.exe --experimental-strip-types node_modules/typescript/bin/tsc -p extensions/<name>/tsconfig.json --noEmit`（三扩展均 exit 0）
- 脚本路径：`node scripts/fix-product-json.mjs` 正确读写本仓库 `product.json`
- 精读：agentLoop 注释、acpRegistry stderr、dev-fast `Test-ClientOutFresh`、product proposals、copy-native 无 index 覆盖
- **已知局限**：未实际启动 Electron 跑通 UI；未代为 git commit
