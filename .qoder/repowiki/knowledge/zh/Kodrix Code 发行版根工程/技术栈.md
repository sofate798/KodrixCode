Node.js ESM 工程（package.json type=module），Electron 42.5.0 作为桌面宿主，esbuild + Gulp 作为主构建管线，TypeScript 6.0-dev（@typescript/native-preview）配合 tsgo 做类型检查，Playwright/Mocha 作为测试框架，Rust 编写的 CLI 工具与 Node 侧 dev-tunnels SDK 协同实现远程隧道能力。