Bash + PowerShell + Node.js (CommonJS, `scripts/package.json` 显式声明 `"type": "commonjs"`)；macOS 构建使用 Apple codesign，Windows 使用 `mock-signtool.bat` 与 `verify-windows-build-env.ps1` 校验签名环境；chat-simulation 使用 TypeScript fixtures 配合 JS 运行器。