Rust (edition 2021) + tokio 全特性异步运行时；clap derive 做 CLI 解析；reqwest + hyper/hyper-util + tokio-tungstenite 提供 HTTP/WebSocket 能力；使用外部 `tunnels` crate（github.com/microsoft/dev-tunnels）作为隧道协议实现；keyring 跨平台密钥存储；russh 通过 `[patch.crates-io]` 指向 microsoft/vscode-russh 分支；Windows/macOS/Linux 通过 `cfg(target_os = ...)` 条件编译平台代码；release profile 启用 strip + thin LTO。