- 每个运行时代码路径（remote / remote/web）各自维护独立的 `package.json`、`.npmrc` 与 `package-lock.json`，实现原生依赖与浏览器依赖的物理隔离。
- 跨运行时代码共享的轻量依赖（xterm、katex、oniguruma、textmate、1ds、tree-sitter-wasm、jschardet、tas-client）在两个包的依赖列表中保持版本号一致，确保行为对齐。