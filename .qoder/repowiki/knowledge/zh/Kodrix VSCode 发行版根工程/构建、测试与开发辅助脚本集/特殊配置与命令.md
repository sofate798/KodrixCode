`dev/build.sh` 支持 `-i`(insiders)、`-l`(latest upstream)、`-o`(跳过构建)、`-p`(打包 assets)、`-s`(跳过 source) 参数，并通过 `dev/build.env` 缓存 `MS_TAG`/`MS_COMMIT`/`RELEASE_VERSION`；macOS 签名需准备 `dev/osx/codesign.env`（模板见 `codesign.env.template`）。