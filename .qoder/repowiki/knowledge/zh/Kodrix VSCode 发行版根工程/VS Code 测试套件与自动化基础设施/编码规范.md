- 测试入口统一使用 `minimist` 解析命令行参数，并以 `--verbose/--headless/--web/--remote/--build` 等布尔/字符串选项控制运行模式。
- 日志采用 `ConsoleLogger` + `FileLogger` 组合并通过 `MultiLogger` 聚合，统一写入 `.build/<suite>/logs` 目录。
- 工作区数据放在 `os.tmpdir()` 下的 `vscsmoke-*` 临时目录，并在进程退出时递归清理。
- 通过 `process.env.VSCODE_REPOSITORY/VSCODE_DEV/VSCODE_CLI` 环境变量切换开发模式，由 `automation` 提供的 `getDevElectronPath/getBuildElectronPath` 解析实际可执行路径。
- 质量版本通过读取 `VSCODE_QUALITY` 枚举映射到 `Quality.Dev/Stable/Insiders/Exploration/OSS`，并在 Electron 与 Web 分支中分别处理。
- 冒烟测试按功能域拆分为 `areas/<feature>/<feature>.test.ts`，并通过 `setup as setupXxxTests` 的工厂函数在 `main.ts` 中按需注册到 `describe` 块。