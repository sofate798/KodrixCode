Mocha (TDD UI) + `@vscode/test-electron` 驱动 Electron/远程实例；Playwright (`@playwright/test`) 用于组件快照测试；webpack 5 用于 Monaco 测试打包；`minimist` 解析 CLI 参数；`graceful-fs`、`semver`、`glob`、`minimatch` 为通用工具；MCP 子项目通过 stdio 暴露自动化能力。